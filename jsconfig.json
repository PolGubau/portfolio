{
    "compilerOptions": {
      "baseUrl": ".", // this must be specified if "paths" is specified.
      "paths": {
        "@comp/": ["src/components/"] // this mapping is relative to "baseUrl"
      }
    }
  }